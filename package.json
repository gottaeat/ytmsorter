{
  "name": "ytmsorter",
  "version": "1.0.0",
  "private": true,
  "description": "Local-first YouTube playlist workbench with browser persistence and a stateless Docker worker",
  "license": "AGPL-3.0-only",
  "type": "module",
  "engines": {
    "node": ">=24"
  },
  "scripts": {
    "start": "node src/index.js",
    "test": "node --test",
    "demo": "node scripts/ui-fixture.mjs",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "check": "node scripts/check.mjs && npm test"
  },
  "dependencies": {
    "express": "5.2.1",
    "youtubei.js": "18.0.0"
  },
  "devDependencies": {
    "prettier": "3.6.2"
  }
}
